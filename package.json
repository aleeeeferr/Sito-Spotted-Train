{
  "name": "spotted-train-root",
  "private": true,
  "scripts": {
    "local": "npm --prefix frontend run local",
    "local:vite": "npm --prefix frontend run local:vite",
    "local:down": "npm --prefix frontend run local:down",
    "prod": "npm --prefix frontend run prod",
    "prod:compose": "npm --prefix frontend run prod:compose",
    "build": "npm --prefix frontend run build"
  }
}
